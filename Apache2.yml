---
- hosts: all
  become: true
  tasks:
    - name: Install apache2
      ansible.builtin.apt:
        name: apache2
        state: present
        update_cache: yes

    - name: Copy index.html to /tmp/
      ansible.builtin.copy:
        src: index.html
        dest: /tmp/
        owner: ubuntu
        group: ubuntu
        mode: '777'
